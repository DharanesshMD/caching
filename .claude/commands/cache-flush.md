# cache-flush

Flush demo caches (in-memory) during development.

This is a placeholder. In a full implementation this would call Redis FLUSH or
other backends. For the demo, manually restart the server to reset in-process
caches.
